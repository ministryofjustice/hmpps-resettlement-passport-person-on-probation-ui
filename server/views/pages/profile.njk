{% extends "../partials/layout.njk" %}
{% from "govuk/components/summary-list/macro.njk" import govukSummaryList %}

{% block content %}

  {% set activePage = 'profile' %}
  {% include "../partials/subnavigation.njk" %}

  <div class="govuk-grid-row govuk-!-padding-bottom-4">
    <div class="govuk-grid-column-two-thirds">
      <h1 class="govuk-heading-xl" data-qa="profile-page-title">Profile</h1>
      <p>This is the contact information we hold for you.</p>
      <p>It is important you keep this information up-to-date. Your probation officer can update these details if anything changes.</p>
    </div>
  </div>

  <div class="govuk-grid-row govuk-!-padding-bottom-8">
    <div class="govuk-grid-column-two-thirds">
    {{ govukSummaryList({
        rows: [
          {
            key: {
              text: "Name"
            },
            value: {
              text: fullName,
              classes: "profile-name"
            }
          },
          {
            key: {
              text: "Email address"
            },
            value: {
              html: email | orElse('Not available'),
              classes: "profile-email"
            }
          },
          {
            key: {
              text: "Mobile"
            },
            value: {
              html: mobile | orElse('Not available'),
              classes: "profile-mobile"
            }
          }
        ]
      }) }}

    </div>
  </div>

{% endblock %}