{% set relatedContentAvailable = page.relatedContent?.length > 0 %}
{% set helpAndAdviceAvailable = page.helpAndAdvice?.length > 0 %}

{% if relatedContentAvailable or helpAndAdviceAvailable %}
  <div class="govuk-grid-column-one-third">
    <div class="gem-c-contextual-sidebar">
      <div class="gem-c-related-navigation">
        <br />
        {% if relatedContentAvailable %}
          <aside class="govuk-prototype-kit-common-templates-related-items">
            <h3 class="govuk-heading-s" id="subsection-title1">{{ t('generic-page-related') }}</h3>
            <nav>
              <ul class="govuk-list govuk-!-font-size-16">
                {% for relatedLink in page.relatedContent %}
                  <li>
                    <a href="{{ relatedLink.link }}"> {{ relatedLink.text }} </a>
                  </li>
                {% endfor %}
              </ul>
            </nav>
          </aside>
        {% endif %}
        {% if helpAndAdviceAvailable %}
          <aside class="govuk-prototype-kit-common-templates-related-items">
            <h3 class="govuk-heading-s" id="subsection-title2">{{ t('generic-page-help') }}</h3>
            <nav>
              <ul class="govuk-list govuk-!-font-size-16">
                {% for helpLink in page.helpAndAdvice %}
                  <li>
                    <a href="{{ helpLink.link }}"> {{ helpLink.text }} </a>
                  </li>
                {% endfor %}
              </ul>
            </nav>
          </aside>
        {% endif %}
      </div>
    </div>
  </div>
{% endif %}
